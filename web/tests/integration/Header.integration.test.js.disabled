import assert from "assert";
import { render } from "@testing-library/preact";
import Header from "../components/Header.astro";

describe.skip("Header", () => {
  // TODO: This test is skipped because it is likely failing due to an incorrect test setup for Astro components.
  // It should be revisited and fixed with a proper Astro testing library.
  it("renders the header", () => {
    const { container } = render(Header);
    assert.ok(container.querySelector("header"));
  });
});
